export interface Address {
    id: string;
    street: string;
    buildingNumber: string;
    flatNumber: string;
    postalCode: string;
    city: string;
    country: string;
    latitude: number;
    longitude: number;
}